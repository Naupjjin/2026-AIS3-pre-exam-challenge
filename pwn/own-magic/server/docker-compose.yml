services:
  pwn-kernel-first-class-mage-exam:
    build:
        context: .
    ports:
      - '13370:13370'
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 1000M  
